# AI-codegen smell audit — 2026-05-20

## Summary

- Commit audited: `1283baacf5168659f53f2dce1924edcc06f7c63e` on branch `master`
- Scope: the 13 files touched by today's 7 merged PRs (#74–#80), focusing only on lines added or modified today — not pre-existing code.
- Total findings: **0** (critical: 0 · major: 0 · minor: 0 · nit: 0)
- False-positive log applied: no — first run on this diff.

## Scope

The audit covered files touched in PRs #74–#80 (commits between a8fb13d and 1283baa):

- `src/auto_updater.py`
- `src/cli/convert.py`
- `src/engine_installer.py`
- `src/gui.py`
- `src/gui_unified.py`
- `src/launcher.py`
- `src/launcher_bridge.py`
- `src/tts_audio.py`
- `src/voice_pack/diarize.py`
- `src/voice_recorder.py`
- `tests/test_auto_updater.py`
- `tests/test_launcher_bridge.py`
- `tests/test_voice_pack_diarize.py`

## Findings

None. All 10 checks passed against today's diff.

| Check | Verdict | Notes |
|---|---|---|
| 1. defensive-checks-for-impossible-cases | clean | Existing `None` guards have correct `Optional`/union annotations. |
| 2. stylistic-drift-within-file | clean | No new mixing of `Path()`/`os.path.join()`, quote styles, or f-string/`.format()` in today's additions. |
| 3. paraphrase-comments | clean | Every new comment names a *why* (RFC, audit reference, upstream behaviour) — no "increment the counter" patterns. |
| 4. single-use-helpers | clean | Eight private helpers called once each (`_downmix_mono`, `_clipping_ratio`, `_ffplay_binary`, etc.) but all have documenting names that carry intent — calibration-immune. |
| 5. generic-names-in-domain-context | clean | New variables use concrete domain terms (`download_url`, `release_data`, `payload`, `reader_error`, `waiter_error`). No `data`/`result`/`temp`/`obj` slipped in. |
| 6. swallowed-errors | clean | Every new `except` block paired with either a log call, a documented intent comment ("safety net against a kernel-level zombie window"), a synthetic event push, or all three. |
| 7. mirror-tests | clean | Three new regression tests (`test_asset_without_browser_download_url_returns_not_available`, `test_sidecar_non_ascii_payload_does_not_corrupt_to_replacement_chars`, `test_hf_token_shim_runs_body_exactly_once_under_concurrent_callers`) all assert specific behavioural outcomes the buggy code would not produce. |
| 8. phantom-todos | clean | No new unowned `TODO`/`FIXME`/`XXX`/`HACK` comments. |
| 9. duplicated-helpers | clean | Three timeout/wait-and-escalate loops with ≥80% structural similarity (`voice_recorder._wait_record`, `voice_recorder._wait_play`, `launcher_bridge._waiter_loop`) — but each has a *distinct documented invariant* (single ffmpeg-record / single ffplay / Chatterbox-runner multi-chunk synthesis), distinct timeout values, distinct UI-signalling patterns. Calibration-immune per the audit's "documented invariants prevent silent divergence" rule. |
| 10. over-typed-primitives | clean | New `_RunnerState.reader_error` / `waiter_error` slots typed as `Optional[BaseException]` — appropriate for the "capture-any-thread-exception" invariant. No `NewType`, `Literal`, `TypedDict`, or heavy decorator stacks introduced. |

## Methodology

Per `.claude/skills/ai-codegen-smell-audit/SKILL.md`'s "Fan out by default" guidance, the 10 checks were bundled into four parallel Explore subordinate runs:

- `{1, 2}` — defensive checks + stylistic drift
- `{3, 4}` — paraphrase comments + single-use helpers
- `{5, 6, 7}` — generic names + swallowed errors + mirror tests
- `{8, 9, 10}` — phantom TODOs + duplicated helpers + over-typed primitives

Each subagent received the scoped file list, the relevant calibration rules, and an instruction to drop findings silently if read-around-the-line context immunised the pattern. The four results merged into this report.

## False-positive log

(empty on first run)

## Patterns observed but not in the checklist

None worth recording. The clean result is consistent with these diffs being human-reviewed audit fixes rather than fresh code generation — every line addressed a specific audit finding with a referenced rationale, so there was less surface for the kinds of habit smells the skill targets.

## What this audit does NOT cover

- Does not look for robustness bugs — that's the `audit` skill's job, run separately on 2026-05-19 and merged in PR #75.
- Does not grade architectural decisions or test for security exploitability.
- Does not flag patterns at trust boundaries (subprocess stdout, network responses, etc.) — those are legitimate.

## Net

Zero findings means today's seven PRs (the audit fix bundle) introduced no detectable AI-codegen smells per the 10-check checklist. The skill's calibration step caught and dropped legitimate patterns (single-use helpers with documenting names, structurally-similar timeout loops with distinct invariants, defensive comments on `except` blocks). No follow-up branch needed.
