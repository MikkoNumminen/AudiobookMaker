# AI-codegen smell audit — 2026-05-17 (v2)

## Summary
- Commit audited: `ec6e294` (origin/master tip, including the PR #67 merge that fixed both first-run findings)
- Scope: `src/` (full tree) plus `tests/` for the mirror-tests check and a repo-wide sweep for phantom-todos
- Total findings: 0 (critical: 0 · major: 0 · minor: 0 · nit: 0)
- False-positive log applied: yes — the v1 report's empty log was read; no findings to suppress because both v1 findings were resolved by fix, not by dismissal

## Why this run exists

The first audit on 2026-05-17 found two minor smells in `src/`:

1. `src/cleanup.py:106` — `os.path.getsize(os.path.join(...))` next to `Path()` elsewhere (stylistic-drift-within-file)
2. `src/auto_updater.py:253` — `data = json.loads(...)` for a GitHub release JSON (generic-names-in-domain-context)

[PR #67](https://github.com/MikkoNumminen/AudiobookMaker/pull/67) fixed both. This second run verifies the fixes held AND that no new smells have crept in since.

## PR #67 fix verification

Both v1 findings confirmed resolved by re-reading the cited lines:

| Finding | v1 state (smell) | v2 state (now) | Verdict |
|---|---|---|---|
| stylistic-drift-within-file at `src/cleanup.py:106` | `total += os.path.getsize(os.path.join(root, f))` | `total += (Path(root) / f).stat().st_size` | FIXED — now consistent with the file's `Path()` idiom |
| generic-names-in-domain-context at `src/auto_updater.py:253` | `data = json.loads(resp.read().decode("utf-8"))` | `release_data = json.loads(resp.read().decode("utf-8"))` | FIXED — domain name flows through the five other reads in `check_for_update` |

## Findings

(none)

## Per-check verdicts

| Check | Verdict | Evidence |
|---|---|---|
| defensive-checks-for-impossible-cases | NO HITS | ~20 `is None` guards spot-read across `src/`; every one guards an `Optional[T]` / `T \| None` annotation, a dict `.get()` return, or a trust-boundary value. |
| stylistic-drift-within-file | NO HITS | PR #67's `Path()` rewrite at `src/cleanup.py:106` confirmed in place. Other candidate files (`system_checks.py`, `engine_installer.py`) use `Path()` throughout — no drift. |
| paraphrase-comments | NO HITS | Sampled comments carry "why" content (constraints, history, architectural intent). No `# increment the counter` shapes found. |
| single-use-helpers | NO HITS | Helper-heavy modules re-checked: `tts_normalizer_fi.py`, `synthesis_orchestrator.py`, `cli/convert.py`. Closures and dispatch helpers exist but carry intent that inlining would lose. |
| generic-names-in-domain-context | NO HITS | PR #67's `release_data` rename confirmed in place at `src/auto_updater.py:253`. The unrelated `data` in `read_pending_marker()` (lines 411-420) remains in its tight 8-line scope — not a smell per the density rule. |
| swallowed-errors | PATTERN PRESENT, CALIBRATION-IMMUNE | **Correction added 2026-05-19 after a post-merge skeptical review.** The original row said "NO HITS" — that was wrong on the literal pattern. `except Exception: pass` / `except Exception: return <default>` shapes exist at `src/engine_registry.py:36-37` (optional-import swallow with comment block above), `src/engine_installer.py:795-796` (best-effort subprocess kill cleanup inside outer error handler), and `src/app_config.py:29-30` (locale fallback, rationale in function docstring). All three are exempted by the *documented intent* calibration rule. Net audit outcome — no actionable smells — stands; the original wording overstated the evidence. |
| mirror-tests | NO HITS | Sampled `test_cleanup.py`, `test_auto_updater.py`, `test_tts_normalizer_fi.py`. Tests assert specific behaviour at multiple inputs. |
| phantom-todos | NO HITS | Repo-wide grep for `# TODO` / `# FIXME` / `# XXX` / `# HACK` returned zero matches (also confirmed by the new `scripts/check_codegen_smells.py` CI gate). |
| duplicated-helpers | NO HITS | Helper-dense `tts_normalizer_fi.py` re-analyzed — similar-looking `_..._sub()` closures each capture distinct semantic context. No cross-module copy-paste pairs found. |
| over-typed-primitives | NO HITS | No `NewType`, `Literal[...]`, or `TypedDict` in `src/`. Codebase uses plain dataclasses and union types. |

## False-positive log

<!--
Append findings the auditor reviewed and dismissed. The next run
reads this section and skips any finding whose `Check + Location`
appears here, unless the line has changed since.

Format:
- `<check-name>` at `<file:line>` — reviewed YYYY-MM-DD by <name>: <why it's a false positive>
-->

(empty — no findings this run to dismiss, and the v1 report's log was empty too)

## Patterns observed but not in the checklist

Re-checking the v1 report's candidate pattern:

- **`__post_init__` clamping on frozen dataclasses** at [src/voice_pack/expression.py:72-78](../../src/voice_pack/expression.py#L72-L78) — still a single observation in this repo. No new sibling instances surfaced. Insufficient evidence to promote to a check.

## Track record

Two runs on one repo over two days — a data point, not an empirical
validation:

- v1 (2026-05-17, first run): 2 minor findings, both real, both fixed in PR #67.
- v2 (2026-05-17, this run): 0 *new* findings on the first read. A skeptical post-merge review on 2026-05-19 then surfaced that the `swallowed-errors` row's "NO HITS" wording was wrong on the literal pattern (see correction above). Net actionable smells in `src/` still zero; auditor wording corrected.

What this single repo run says about the skill: the audit cycle works
(it caught its own blind spot once a fresh reviewer cross-checked),
not that the skill is empirically validated. That would need
adversarial runs on multiple repos with intentional plants.

## Audit method

Same 4-parallel-sub-agent pattern as the v1 run. The repo's working tree was at origin/master (`ec6e294`) including the PR #67 fixes. Calibration rules applied before recording any finding. Every cited line was spot-read. No findings to write.

## Suggested next step

None. The codebase is at zero findings under the current ten-check schema. The next meaningful action is a fresh AI-heavy diff to test the skill against — the codebase has had careful human review and is no longer a useful test bed for the LLM-only checks. The CI greppable gate at `.github/workflows/codegen-smell-audit.yml` will catch regressions on the four greppable checks going forward.
