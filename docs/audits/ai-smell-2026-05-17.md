# AI-codegen smell audit — 2026-05-17

## Summary
- Commit audited: `54536b1` (origin/master, the merge of PR #66 that added this skill); working tree at local `658b5c2` is content-identical
- Scope: `src/` (full tree) plus `tests/` for the mirror-tests check and a repo-wide sweep for phantom-todos
- Total findings: 2 (critical: 0 · major: 0 · minor: 2 · nit: 0)
- False-positive log applied: no — first run

## Findings

| Check | Severity | Location | Snippet | Suggested action |
|---|---|---|---|---|
| stylistic-drift-within-file | minor | [src/cleanup.py:106](../../src/cleanup.py#L106) | `total += os.path.getsize(os.path.join(root, f))` | Rewrite the inner expression with `Path()` to match the rest of the file; the surrounding module builds every other path via `Path`. |
| generic-names-in-domain-context | minor | [src/auto_updater.py:253](../../src/auto_updater.py#L253) | `data = json.loads(resp.read().decode("utf-8"))` | Rename to `release_data` (or similar) — the rest of the function reads `data.get("tag_name")`, `data.get("assets", [])`, which are clearly GitHub release fields. |

## Findings by severity

### Critical
- (none)

### Major
- (none)

### Minor
- [src/cleanup.py:106](../../src/cleanup.py#L106) — `os.path.getsize(os.path.join(root, f))` while the rest of the same module uses `Path()` for path construction. (stylistic-drift-within-file)
- [src/auto_updater.py:253](../../src/auto_updater.py#L253) — `data = json.loads(...)` for a GitHub release JSON response; the surrounding code makes the domain obvious (`tag_name`, `assets`). (generic-names-in-domain-context)

### Nit
- (none)

## Per-check verdicts (zero-finding checks)

The eight checks that yielded zero findings are recorded explicitly so the next run knows the codebase was actually scanned — empty sections are not skipped sections.

| Check | Verdict | Notes |
|---|---|---|
| defensive-checks-for-impossible-cases | NO HITS | ~100 `is None` guards inspected across `src/`; every one guards a `T \| None` / `Optional[T]` annotation, a dict `.get()` return, or a trust-boundary value. None contradict the type signature. |
| paraphrase-comments | NO HITS | Sampled comment density across `src/`. Comments carry "why" content (constraints, history, architectural intent) — no `# increment the counter` shape found. |
| single-use-helpers | NO HITS | Call-count audit on `synthesis_orchestrator.py`, `tts_normalizer_fi.py`, `auto_updater.py`, `cli/convert.py`, `engine_installer.py`. Helpers called exactly once *within their enclosing scope* exist (regex-substitution closures in `tts_normalizer_fi.py`, dispatch branches like `_run_chatterbox` in `cli/convert.py`), but they all carry intent the inline form would lose (the calibration rule for documenting names applies). |
| swallowed-errors | NO HITS | No bare `except: pass` or unlogged `except Exception:` in `src/`. Every handler is typed (`except PermissionError:`, `except FileNotFoundError:`, etc.) or logs + recovers with a rationale comment. |
| mirror-tests | NO HITS | Spot-read `test_tts_normalizer_fi.py`, `test_cleanup.py`, `test_app_config.py`, `test_render_cli_help.py`, `test_tts_audio.py`, `test_tts_base.py`. Tests assert at multiple inputs / edge cases against known-good values; none re-run implementation logic in the assertion. |
| phantom-todos | NO HITS | Exhaustive grep for `# TODO` / `# FIXME` / `# XXX` / `# HACK` across `src/`, `tests/`, `scripts/`, `data/` returned zero matches. |
| duplicated-helpers | NO HITS | Helper-heavy modules read in full. The many `_..._sub(m)` closures in `tts_normalizer_fi.py` look similar at a glance but each captures a different lookup table / validation context. No cross-module copy-paste pairs found. |
| over-typed-primitives | NO HITS | No `NewType`, `Literal[...]`, or `TypedDict` usage in `src/`. Codebase uses plain dataclasses and `str | None` unions. |

## False-positive log

<!--
Append findings the auditor reviewed and dismissed. The next run
reads this section and skips any finding whose `Check + Location`
appears here, unless the line has changed since.

Format:
- `<check-name>` at `<file:line>` — reviewed YYYY-MM-DD by <name>: <why it's a false positive>
-->

(empty on first run)

## Patterns observed but not in the checklist

One pattern recurred in `src/` that does not match any of the ten checks but is worth recording as evidence for a possible future check:

- **`__post_init__` clamping on frozen dataclasses.** Example: [src/voice_pack/expression.py:72-78](../../src/voice_pack/expression.py#L72-L78). A `frozen=True` dataclass whose `__post_init__` uses `object.__setattr__` to clamp values rather than validate or raise. Signals "I wanted validation but did not want to write a custom `__init__`". A future check `post-init-mutation-workaround` could target this. Single observation, single repo — not enough evidence to promote.

## Audit method

Run by the `ai-codegen-smell-audit` skill on 2026-05-17. Four parallel sub-agents handled the ten checks grouped by analysis type:

- Greppable patterns (defensive-checks, swallowed-errors, over-typed-primitives)
- File-level analysis (stylistic-drift, generic-names)
- Structural analysis (single-use-helpers, duplicated-helpers)
- Text patterns (paraphrase-comments, mirror-tests, phantom-todos)

Each sub-agent returned canonical-format findings; the aggregate above is the union of their reports after calibration rules were applied. Every cited line was spot-read before recording. The two minor findings match the skill's own pre-shipping calibration table exactly — vindicating that the calibration was honest, and confirming the skill's own observation that "this codebase has had careful human review pass over it."

## Suggested next step

Both findings are minor and bundleable. A small `chore: clean up two ai-codegen smells` branch could fix both in one commit:

1. `src/cleanup.py:106` — rewrite the `os.path.getsize(os.path.join(...))` line to use `Path()`.
2. `src/auto_updater.py:253` — rename `data` → `release_data` (or similar) for the GitHub release JSON.

Or leave both as-is — they are real but low-impact. The user picks.
