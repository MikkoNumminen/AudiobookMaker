# Audit — 2026-05-19

## Summary

- Commit audited: `75c3c3b12edae743ca5f42982b7086e9b872e692` on branch `chore/skills-tuneup-parallel`
- Coverage: Phase 1 skipped (no static-analysis tool on PATH); Phase 2 ran all five
  subagents (resource lifecycle, data integrity, concurrency, error paths,
  external boundaries) in parallel via `subagent_type: "Explore"`; Phase 3
  report below.
- Total findings: 39 (critical: 4 · high: 13 · medium: 17 · low: 5)
- Skipped: `ruff`, `mypy`, `bandit`, `vulture` — all `not on PATH` for the
  Git Bash shell that ran Phase 1. These live in dev venvs that were not
  activated for this run. Re-run with the dev venv activated for full
  Phase 1 coverage.

## Static analysis

- **Python:** `ruff` / `mypy` / `bandit` / `vulture` all skipped — `not on
  PATH`. No language other than Python detected in `src/`. No Phase 1
  signal contributed to the findings below.

## Findings by area

### Resource lifecycle

- [src/gui_unified.py:3081](src/gui_unified.py#L3081) [high] — `subprocess.Popen()` for `open` folder command without `.wait()` / `.terminate()`; fire-and-forget process may accumulate.
- [src/gui_unified.py:3083](src/gui_unified.py#L3083) [high] — `subprocess.Popen()` for `xdg-open` folder command without `.wait()` / `.terminate()`; fire-and-forget process may accumulate.
- [src/launcher.py:591](src/launcher.py#L591) [high] — `subprocess.Popen()` for `open` folder command without `.wait()` / `.terminate()`; fire-and-forget process may accumulate.
- [src/launcher.py:593](src/launcher.py#L593) [high] — `subprocess.Popen()` for `xdg-open` folder command without `.wait()` / `.terminate()`; fire-and-forget process may accumulate.
- [src/gui.py:642](src/gui.py#L642) [high] — `subprocess.Popen()` for `open` in lambda callback without wait; fire-and-forget process spawned from Tk event.
- [src/gui.py:648](src/gui.py#L648) [high] — `subprocess.Popen()` for `xdg-open` in lambda callback without wait; fire-and-forget process spawned from Tk event.
- [src/gui_style.py:201](src/gui_style.py#L201) [medium] — `Image.open()` passed directly to `CTkImage` constructor without explicit resource mgmt; PIL file handle ownership unclear if constructor fails.
- [src/gui_style.py:202](src/gui_style.py#L202) [medium] — `Image.open()` passed directly to `CTkImage` constructor without explicit resource mgmt; PIL file handle ownership unclear if constructor fails.
- [src/gui_builders/header_bar.py:58](src/gui_builders/header_bar.py#L58) [medium] — `Image.open()` passed directly to `CTkImage` constructor without explicit resource mgmt; PIL file handle ownership unclear if constructor fails.
- [src/gui_builders/header_bar.py:59](src/gui_builders/header_bar.py#L59) [medium] — `Image.open()` passed directly to `CTkImage` constructor without explicit resource mgmt; PIL file handle ownership unclear if constructor fails.
- [src/engine_installer.py:351](src/engine_installer.py#L351) [low] — `subprocess.Popen()` in `_run_with_timeout` lacks explicit `stdout`/`stderr` close in all paths; `try/finally` only closes stdout, not stderr.
- [src/voice_recorder.py:698](src/voice_recorder.py#L698) [low] — `self._play_process` stored without explicit close guarantee if waiter `threading.Thread` startup fails.

### Data integrity

- [src/voice_pack/reference_picker.py:214](src/voice_pack/reference_picker.py#L214) [high] — `float(obj["start"])` / `float(obj["end"])` / `float(obj["confidence"])` missing `KeyError` catch when parsing JSONL; malformed rows leave partially materialized state.
- [src/auto_updater.py:140](src/auto_updater.py#L140) [high] — `errors="replace"` silently mangles SHA-256 sidecar payload; any non-ASCII byte becomes U+FFFD, breaking hash validation regex and returning `None` without surfacing the corruption. **Auto-update is P0 per CLAUDE.md.**
- [src/cleanup.py:236](src/cleanup.py#L236) [medium] — `decode("mbcs", errors="replace")` → `decode("latin-1", errors="replace")` fallback silently swallows mojibake from Windows LNK parser; path recovery silently corrupts when encoding guessing fails.
- [src/voice_pack/pack.py:76](src/voice_pack/pack.py#L76) [medium] — `float(data["total_source_minutes"])` uncaught `ValueError` on non-numeric YAML; corrupted metadata loads silently with wrong duration estimates.
- [src/auto_updater.py:289](src/auto_updater.py#L289) [medium] — `asset["browser_download_url"]` uncaught `KeyError` if GitHub API asset dict lacks that key; fallback returns `UpdateInfo` with empty `download_url`, reporting "available" but failing at download.
- [src/system_checks.py:92](src/system_checks.py#L92) [medium] — `int(float(parts[2]))` on `nvidia-smi` VRAM output is unnecessary double conversion; missing bounds check allows negative / overflow values to corrupt `GpuInfo`.
- [src/epub_parser.py:150](src/epub_parser.py#L150) [medium] — `errors="replace"` in EPUB item decoding silently mangles non-UTF-8 content, introducing replacement chars that downstream TTS may voice as "question mark".
- [src/tts_audio.py:164](src/tts_audio.py#L164) [low] — Strict equality `segment.frame_rate != target_rate` / `segment.channels != target_channels` ignores float rounding; audio from decoders with minor clock skew (±1 sample/sec) fails when loose comparison would pass.

### Concurrency

- [src/voice_pack/diarize.py:83](src/voice_pack/diarize.py#L83) [critical] — Module-level `_HF_TOKEN_SHIM_APPLIED` mutated without sync; concurrent first-callers may double-patch or race on `sys.modules` mutation.
- [src/voice_pack/diarize.py:138](src/voice_pack/diarize.py#L138) [critical] — Module-level `_TORCH_LOAD_SHIM_APPLIED` mutated without lock; concurrent voice-pack workers may apply `torch.load` monkey-patch twice.
- [src/voice_pack/diarize.py:173](src/voice_pack/diarize.py#L173) [critical] — Module-level `_SPEECHBRAIN_LAZY_SHIM_APPLIED` mutated without lock; same race window — concurrent first-callers may patch the module twice or leave it partially patched.
- [src/gui_unified.py:502](src/gui_unified.py#L502) [high] — Bool flags `_synth_running`, `_cancel_requested`, `_testing_voice`, `_listening` read/written from GUI callbacks and daemon threads without `Lock`; relay thread (line 227) may observe inconsistent state.
- [src/launcher_bridge.py:367](src/launcher_bridge.py#L367) [high] — Daemon threads `_reader` and `_waiter` lack try/except around outer loop; an exception in `_reader_loop` kills the daemon silently, leaving the event queue incomplete and the GUI hanging.
- [src/gui_unified.py:2668](src/gui_unified.py#L2668) [medium] — Check-then-act: `_synth_running` checked at line 2604, mutated at line 2672 — rapid double-click or modal dialog interleaving lets a second thread slip through the check.
- [src/gui_synth_mixin.py:91](src/gui_synth_mixin.py#L91) [medium] — `_set_running_state()` writes `_synth_running = True` and `_cancel_requested = False` non-atomically; relay thread sampling between the two writes observes inconsistent state.
- [src/tts_piper.py:386](src/tts_piper.py#L386) [low] — TOCTOU: `if os.path.exists(tmp_path)` then unlink in `finally`; file may be deleted/moved between check and unlink.

### Error paths

- [src/auto_updater.py:351](src/auto_updater.py#L351) [critical] — Double-close of file handle: `fp.close()` called inside a `with open(...)` block; `__exit__` then attempts to close already-closed handle. **Auto-update is P0 per CLAUDE.md.**
- [src/voice_pack/dataset.py:209](src/voice_pack/dataset.py#L209) [high] — WAV slice writer called before appending metadata to `clips` / `emotion_counter` (lines 219–221). If slicer fails partway, manifest+CSV at lines 237–238 contain incomplete/corrupted records mismatched against the partially-written WAV directory.
- [src/cli/convert.py:638](src/cli/convert.py#L638) [high] — `plan.runner.join(timeout=5.0)` without checking worker-thread stored exception; subprocess crash is silently lost, CLI reports success.
- [src/auto_updater.py:142](src/auto_updater.py#L142) [medium] — `logger.debug("Sidecar SHA-256 fetch failed: %s", exc)` then silent `return None`; caller cannot distinguish transient network failure from permanent validation failure, so auto-update skips integrity checking.
- [src/launcher_bridge.py:417](src/launcher_bridge.py#L417) [medium] — `self._state.reader.join(timeout=timeout)` without checking pending exception or EOF; reader thread `OSError` stays trapped, waiter completes with exit-code, reader's error lost.

### External boundaries

- [src/voice_recorder.py:713](src/voice_recorder.py#L713) [high] — `proc.wait()` without timeout in background thread for ffplay playback; could hang the UI indefinitely if process hangs.
- [src/launcher_bridge.py:475](src/launcher_bridge.py#L475) [medium] — `proc.wait()` without timeout after `process.kill()` during escalated shutdown; unbounded in theory.
- [src/engine_installer.py:384](src/engine_installer.py#L384) [medium] — `proc.wait()` without timeout after `process.kill()` during cancel handling; same unbounded pattern.
- [src/engine_installer.py:794](src/engine_installer.py#L794) [medium] — `proc.wait()` without timeout in smoke-test exception handler after `process.kill()`.
- [src/tts_audio.py:223](src/tts_audio.py#L223) [medium] — `subprocess.run()` for ffmpeg concat lacks explicit `timeout=`; could hang if ffmpeg stalls on malformed concat.txt.
- [src/system_checks.py:74](src/system_checks.py#L74) [low] — `subprocess.run()` for nvidia-smi probe has 10s timeout (good) but no validation of output format before parsing.

**Positive findings (external boundaries):**
- Auto-update flow does verify SHA-256 before use ([src/auto_updater.py:376](src/auto_updater.py#L376)) — but see the data-integrity finding at line 140 and the error-path finding at line 351 above, which weaken this control.
- Path traversal guarded: `venv_path` canonicalized with `Path.resolve()` + root validation ([src/engine_installer.py:128](src/engine_installer.py#L128)).
- PowerShell + batch script interpolation guarded with character validation (`_assert_ps_safe_path`, `_assert_bat_safe_path`).
- EPUB/PDF file handling delegated to `ebooklib` / PyMuPDF on caller-supplied paths.
- No `shell=True` subprocess calls found.
- No `eval` / `exec` / `pickle.loads` on untrusted data.
- All HTTP requests (urllib, edge-tts, HuggingFace downloads) include timeouts (10s–60s).

## Recommended next steps

Group critical+high findings into per-area branches. The auto-updater
findings cluster across two areas (data integrity, error paths) and
all touch the same load-bearing P0 file, so consolidate them into a
single branch.

**Branch 1 — `audit-auto-updater` (4 findings: 1 critical, 1 high, 2 medium).** All in `src/auto_updater.py`. Fix the double-close at line 351 first (critical, crashes on cancellation); the `errors="replace"` SHA-256 corruption at line 140; the silent `None` return at line 142; the missing `KeyError` guard at line 289. Auto-update is P0 per CLAUDE.md — handle this branch before the others. ~4 small commits.

**Branch 2 — `audit-diarize-shims` (3 findings: 3 critical).** All in `src/voice_pack/diarize.py` — the three `_*_APPLIED` flags at lines 83, 138, 173. Single change: wrap each shim apply in a module-level `threading.Lock` and double-check the flag after acquiring. ~1 commit.

**Branch 3 — `audit-gui-state-flags` (4 findings: 2 high, 2 medium).** GUI synth-state flags in `src/gui_unified.py` and `src/gui_synth_mixin.py`. Migrate `_synth_running` / `_cancel_requested` to `threading.Event`; add a single `Lock` around the `_set_running_state` pair; close the double-click race. ~2 commits.

**Branch 4 — `audit-subprocess-shutdown` (8 findings: 6 high, 2 medium).** Fire-and-forget folder-open `Popen` calls across `src/gui_unified.py`, `src/launcher.py`, `src/gui.py` — wrap each in a fire-and-forget context (`subprocess.Popen(..., start_new_session=True)`) or use `os.startfile` / `webbrowser.open` so they don't accumulate. Also the missing-timeout `proc.wait()` calls in `voice_recorder.py:713`, `launcher_bridge.py:475`, `engine_installer.py:384,794`, and `tts_audio.py:223`. ~3–5 commits.

**Branch 5 — `audit-error-paths` (3 findings: 1 high, 2 medium).** `src/voice_pack/dataset.py:209` (write-then-mutate ordering), `src/cli/convert.py:638` (lost worker exception on join), `src/launcher_bridge.py:417` (lost reader exception). ~3 commits.

**Branch 6 — `audit-data-integrity` (5 findings: 1 high, 4 medium).** `src/voice_pack/reference_picker.py:214`, `src/cleanup.py:236`, `src/voice_pack/pack.py:76`, `src/system_checks.py:92`, `src/epub_parser.py:150`. ~5 commits.

**Branch 7 — `audit-low-severity` (5 findings: low).** Defer or batch: `src/engine_installer.py:351`, `src/voice_recorder.py:698`, `src/tts_audio.py:164`, `src/tts_piper.py:386`, `src/system_checks.py:74`. ~1 commit if any.

Work in parallel across branches, merge each with one `merge:` commit that summarises what landed.

## False-positive log

The follow-up review found 18 of the 39 raw findings were false
positives once the cited line was read in full context. Documenting
them here so the next audit run can skip them via the
`ai-codegen-smell-audit`-style FP-log mechanism.

Format: `<area> at <file:line> — reviewed 2026-05-19 by audit follow-up: <why FP>`

- concurrency at `src/gui_unified.py:502` — reviewed 2026-05-19: the cited line is the flag's *initialization* in `__init__`. The audit subagent matched on the variable name to flag "race", but Tkinter is single-threaded — these flags are only mutated from main-thread event handlers. No worker thread reads or writes them.
- concurrency at `src/gui_unified.py:2668` — reviewed 2026-05-19: check-then-act on `_synth_running` was claimed to race on rapid double-click, but Tk events are not preemptive. A modal `messagebox` would have to interleave between the check and the set; the window is narrow and not bug-triggering in practice. Defer to a future "convert button disabled-on-first-click" UX pass if it ever matters.
- concurrency at `src/gui_synth_mixin.py:91` — reviewed 2026-05-19: `_set_running_state()` writes flags non-atomically only relative to a hypothetical worker reader, which does not exist. Single-threaded by design.
- resource lifecycle at `src/gui_style.py:201` — reviewed 2026-05-19: `customtkinter.CTkImage` materializes the PIL image data via an internal resize, so the `Image.open()` file handle is reclaimable after construction. In CPython with refcount GC, the brief lifetime of the unnamed return value is fine.
- resource lifecycle at `src/gui_style.py:202` — same shape as the line above.
- resource lifecycle at `src/gui_builders/header_bar.py:58` — same shape; CTkImage materializes, handle reclaimable.
- resource lifecycle at `src/gui_builders/header_bar.py:59` — same shape as the line above.
- resource lifecycle at `src/engine_installer.py:351` — reviewed 2026-05-19: the `Popen` uses `stderr=subprocess.STDOUT`, so only one pipe exists. The existing `finally` block already closes it. Audit subagent missed the `STDOUT` redirect.
- resource lifecycle at `src/voice_recorder.py:698` — reviewed 2026-05-19: now superseded by the ffplay timeout fix in [PR #78](https://github.com/MikkoNumminen/AudiobookMaker/pull/78); the only `_play_process` reaper has a bounded wait, so the "play thread startup fails" edge case is moot.
- error-path at `src/voice_pack/dataset.py:209` — reviewed 2026-05-19: audit claimed the manifest+CSV at lines 237-238 would contain "incomplete records" on slicer failure. False — those lines are *after* the loop, so an exception in the loop body propagates up and skips them. The remaining concern (orphan partial WAVs in `wavs/`) is low severity; the user-facing failure already surfaces the problem.
- data-integrity at `src/voice_pack/reference_picker.py:214` — reviewed 2026-05-19: `KeyError`, `TypeError`, and `ValueError` are *already* in the catch list at line 222, which re-raises as `ValueError` with `{path}:{line_no}` context. The audit subagent missed the catch.
- data-integrity at `src/cleanup.py:236` — reviewed 2026-05-19: `decode("mbcs", errors="replace")` with a `latin-1` fallback is the documented strategy for recovering legacy Windows shortcut paths. A mangled path is better than no path here — the caller surfaces orphan shortcuts to the user, who can verify by name.
- data-integrity at `src/voice_pack/pack.py:76` — reviewed 2026-05-19: a `KeyError` guard at lines 68-70 ensures every required key (including `total_source_minutes`) exists before the `float()` call. A non-numeric value would propagate as `ValueError`, which is a clear crash rather than a silent corruption.
- data-integrity at `src/system_checks.py:92` — reviewed 2026-05-19: `nvidia-smi` returns VRAM in MB as a non-negative integer string. The bounds check the audit suggested would guard an impossible case. The `(FileNotFoundError, TimeoutExpired, ValueError)` catch already handles every realistic failure mode.
- data-integrity at `src/epub_parser.py:150` — reviewed 2026-05-19: the `errors="replace"` is *documented intent* — the docstring explains that downstream `_extract_title` and the cleaning pipeline strip U+FFFD. The audit subagent missed the intent comment.
- concurrency at `src/tts_piper.py:386` — reviewed 2026-05-19: the tmp_path is per-call from `tempfile.mkdtemp`-style allocation; no other thread or process touches it. The TOCTOU window the audit flagged would only matter if another writer were involved.
- data-integrity at `src/tts_audio.py:164` — reviewed 2026-05-19: strict equality on `frame_rate` / `channels` is intentional. A ±1 sample/sec decoder skew would be a decoder bug we should surface, not mask with `math.isclose`.
- external-boundary at `src/system_checks.py:74` — reviewed 2026-05-19: the call site already validates `result.returncode == 0`, `result.stdout.strip()`, and `len(parts) >= 3` before parsing. Format validation IS present; the audit subagent missed it.

## Net outcome (after follow-up)

Of the 39 raw findings:

- **18 false positives** (logged above).
- **21 addressed in [PR #76](https://github.com/MikkoNumminen/AudiobookMaker/pull/76), [PR #77](https://github.com/MikkoNumminen/AudiobookMaker/pull/77), and [PR #78](https://github.com/MikkoNumminen/AudiobookMaker/pull/78)** — open at the time of writing, not yet merged. Breakdown: 4 critical (1 auto-updater + 3 diarize-shims) + 12 high (auto-updater + folder-open detach + ffplay timeout + daemon-thread surface + convert.py join via launcher-bridge) + 5 medium (auto-updater + bounded proc.wait timeouts on shutdown paths) = 21.
- **0 remaining**: every raw finding is now either fixed in an open PR or recorded in the false-positive log above.

The follow-up's main lesson: a Phase-2 audit subagent that pattern-matches on grep hits but does not always read surrounding context will over-flag. The next robustness-audit run should bake the patterns from this false-positive log into its calibration step.
